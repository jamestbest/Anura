//
// Created by jamescoward on 04/11/2023.
//
#ifndef ATOMIC_HELPER_MATH_H
#define ATOMIC_HELPER_MATH_H

#include "../Commons.h"

typedef unsigned int uint;

int max(int a, int b);
uint umax(uint a, uint b);
size_t smax(size_t a, size_t b);

int min(int a, int b);
uint umin(uint a, uint b);
size_t smin(size_t a, size_t b);

#endif //ATOMIC_HELPER_MATH_H
